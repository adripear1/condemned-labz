{% comment %} This file is auto-generated. Do not edit this file. {% endcomment %}
<div class='ss-slideshow__controls'>
  <button
    class='ss-slideshow-control ss-slideshow-control--previous ss-button-secondary'
    aria-label='Previous slide'
  >
    {%- render 'ss-icon-v-1-0', icon: 'previous', width: '10', height: '14', view_box: '0 0 10 14', fill: 'none' %}
  </button>

  {% case block.settings.pagination_style %}
    {% when 'dots' %}
      {% for block in section.blocks %}
        <button
          class='ss-slideshow-control ss-slideshow-controls__link ss-button-unstyled{% if forloop.first %} ss-slideshow-controls__link--active{% endif %}'
          aria-label='Slide {{ forloop.index }} of {{ forloop.length }}'
          data-slide-number='{{ forloop.index0 }}'
        >
          <span class='ss-slideshow-controls__dot'></span>
        </button>
      {% endfor %}

    {% when 'counter' %}
      <span class='ss-current-slide-indicator'>1/{{ section.blocks.size }}</span>

    {% when 'numbers' %}
      {% for block in section.blocks %}
        <button
          class='ss-slideshow-control ss-slideshow-controls__link ss-button-unstyled{% if forloop.first %} ss-slideshow-controls__link--active{% endif %}'
          aria-label='Slide {{ forloop.index }} of {{ forloop.length }}'
          data-slide-number='{{ forloop.index0 }}'
        >
          {{ forloop.index }}
        </button>
      {% endfor %}
  {% endcase %}
  <button
    class='ss-slideshow-control ss-slideshow-control--next ss-button-secondary'
    aria-label='Next slide'
  >
    {%- render 'ss-icon-v-1-0', icon: 'next', view_box: '0 0 10 14', fill: 'none' -%}
  </button>
  {% if section.settings.autoplay %}
    <button
      type='button'
      class='ss-slideshow-control ss-slideshow-control--autoplay ss-button-secondary'
      aria-label='Pause slideshow'
    >
      {%- render 'ss-icon-v-1-0', icon: 'pause', view_box: '0 0 10 14', fill: 'none' -%}
      {%- render 'ss-icon-v-1-0', icon: 'play', fill: 'none', view_box: '0 0 10 14' -%}
    </button>
  {% endif %}
</div>

{% schema %}
{
  "name": "Slideshow controls 1-0",
  "class": "ss-block",
  "settings": [
    {
      "type": "select",
      "id": "pagination_style",
      "options": [
        {
          "value": "dots",
          "label": "Dots"
        },
        {
          "value": "counter",
          "label": "Counter"
        },
        {
          "value": "numbers",
          "label": "Numbers"
        }
      ],
      "label": "Pagination style",
      "default": "counter"
    }
  ]
}
{% endschema %}
